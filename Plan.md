
# Plan/Design

## 注意

- ~~複数のフォーマット(例: .htmlと.pdfの両方)を出力できるようにしてはいけない、Debugが難しくなるから。~~ → html_pdfモードを追加。確かに、単一出力を2つ変更に変換町できるが、

- inputもoutputも最終outputフォルダしないといけないので、VSCode再起動するたびに、最終フォルダの全てのhtmlを再度pdf化しようとしまう負担も心配。

## GDriveの監視が必要

- Polling
